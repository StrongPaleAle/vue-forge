mutation addLabelToTask($taskId: ID!, $label: String, $color: String) {
  taskUpdate(
    filter: { id: $taskId }, 
    data: { labels: { create: [{label: $label, color: $color}] }}
    ) {
    id
    labels{
      items{
        id
        label
        color
      }
    }
  }
}