mutation attachImageToTask($id: ID!, $imageId: ID!) {
  taskUpdate(
    filter: { id: $id }
    data: { image: { reconnect: { id: $imageId } } }
  ) {
    id
    image {
      id
      downloadUrl
    }
  }
}